# cscd_cohort4_aiml_repo

```bash
mkdir cscd
cd cscd
git clone https://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@github.com/vamsi-kundla/cscd_cohort4_aiml_repo.git
```
```bash
sudo apt install -y python3-pip
python3 -m pip install virtualenv
virtualenv <env-name>
source <env-name>/bin/activate
pip install bs4 lxml pandas numpy sklearn matplotlib
```
